
  <manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="vision.zeus.plugins.zeuspushnotifications">
    <application>
      <service android:name="vision.zeus.services.CustomFirebaseMessagingService" android:stopWithTask="false">
        <meta-data android:name="mainClass" android:value="@string/main_class" />
        <intent-filter>
          <action android:name="com.google.firebase.MESSAGING_EVENT" />
        </intent-filter>
      </service>
    </application>
  </manifest>
  